/**
 * Contains model classes that represent the data of the Address Book application.
 * Includes the {@link com.example.addressbook.model.Contact} class that represents a contact,
 * alongside DAO classes that handle the data access.
 */
package com.example.addressbook.model;